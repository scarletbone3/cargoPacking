export interface KonvaConfig {
  width: number;
  height: number;
}

export interface RectangleConfig {
  width: number,
  height: number,
  x: number;
  y: number;
  fill: string;
  stroke: string;
}
